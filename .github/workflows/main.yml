name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run eslint check
      run: |
        echo Starting eslint..
        make lint
        echo
